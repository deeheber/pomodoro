#!/bin/sh
set -eu
APP_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if ! command -v python3 >/dev/null 2>&1; then
    echo 'Python 3 is required. Install it, then run ./pomodoro again.' >&2
    exit 1
fi
exec python3 "$APP_DIR/pomodoro.py" "$@"
